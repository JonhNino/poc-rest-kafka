global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'poc-rest-kafka'
    metrics_path: '/q/metrics'
    static_configs:
      - targets: ['poc-rest-kafka:8080']
        labels:
          application: 'poc-rest-kafka'

